add_executable(register_flow register_flow.cpp)
add_executable(login_flow login_flow.cpp)
add_executable(public_key_flow public_key_flow.cpp)
add_executable(message_send_flow message_send_flow.cpp)
add_executable(conversation_poll_flow conversation_poll_flow.cpp)

target_compile_definitions(register_flow PRIVATE ANONIM_TESTING)
target_compile_definitions(login_flow PRIVATE ANONIM_TESTING)
target_compile_definitions(public_key_flow PRIVATE ANONIM_TESTING)
target_compile_definitions(message_send_flow PRIVATE ANONIM_TESTING)
target_compile_definitions(conversation_poll_flow PRIVATE ANONIM_TESTING)

target_include_directories(register_flow PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/../../include ${CMAKE_CURRENT_SOURCE_DIR}/..)
target_include_directories(login_flow PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/../../include ${CMAKE_CURRENT_SOURCE_DIR}/..)
target_include_directories(public_key_flow PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/../../include ${CMAKE_CURRENT_SOURCE_DIR}/..)
target_include_directories(message_send_flow PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/../../include ${CMAKE_CURRENT_SOURCE_DIR}/..)
target_include_directories(conversation_poll_flow PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/../../include ${CMAKE_CURRENT_SOURCE_DIR}/..)

target_link_libraries(register_flow PRIVATE anonim_backend_core)
target_link_libraries(login_flow PRIVATE anonim_backend_core)
target_link_libraries(public_key_flow PRIVATE anonim_backend_core)
target_link_libraries(message_send_flow PRIVATE anonim_backend_core)
target_link_libraries(conversation_poll_flow PRIVATE anonim_backend_core)

set_target_properties(register_flow PROPERTIES RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/tests/scenarios)
set_target_properties(login_flow PROPERTIES RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/tests/scenarios)
set_target_properties(public_key_flow PROPERTIES RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/tests/scenarios)
set_target_properties(message_send_flow PROPERTIES RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/tests/scenarios)
set_target_properties(conversation_poll_flow PROPERTIES RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/tests/scenarios)